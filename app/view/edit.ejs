<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Laporan - SIAPPRODI TI UMY</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Gradient Hijau Tua Khas UMY */
            background: linear-gradient(135deg, #006837 0%, #004d29 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            padding: 20px;
        }

        .edit-card {
            background: #ffffff;
            border: none;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 500px;
            padding: 40px;
            border-top: 8px solid #f9c909; /* Aksen Kuning UMY */
        }

        .brand-logo {
            font-size: 1.8rem;
            font-weight: 800;
            color: #006837;
            margin-bottom: 5px;
            display: block;
            text-align: center;
        }

        .edit-title {
            text-align: center;
            color: #6c757d;
            font-size: 0.9rem;
            margin-bottom: 30px;
            font-weight: 500;
        }

        .form-label {
            font-weight: 600;
            font-size: 0.85rem;
            color: #2d3436;
        }

        .form-control, .form-select {
            border-radius: 10px;
            padding: 12px 15px;
            border: 1px solid #dee2e6;
            background-color: #f8f9fa;
        }

        .form-control:focus, .form-select:focus {
            box-shadow: 0 0 0 4px rgba(0, 104, 55, 0.15);
            border-color: #006837;
            background-color: #ffffff;
        }

        .btn-save {
            background: #006837;
            border: none;
            border-radius: 10px;
            padding: 12px;
            font-weight: 700;
            color: #ffffff;
            transition: all 0.3s;
        }

        .btn-save:hover {
            background: #004d29;
            color: #f9c909;
            transform: translateY(-2px);
        }

        .btn-cancel {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 10px;
            padding: 12px;
            font-weight: 600;
            color: #6c757d;
            text-decoration: none;
            display: block;
            text-align: center;
            transition: all 0.3s;
        }

        .btn-cancel:hover {
            background: #e9ecef;
            color: #333;
        }

        .footer-text {
            text-align: center;
            margin-top: 25px;
            font-size: 0.75rem;
            color: #95a5a6;
        }
    </style>
</head>
<body>

    <div class="edit-card">
        <div class="brand-logo">
            <i class="fas fa-edit me-2"></i>EDIT<span style="color: #f9c909;">LAPORAN</span>
        </div>
        <p class="edit-title">Perbarui informasi aspirasi atau pengaduan Anda</p>

        <form action="/update/<%= data.id_aspirasi %>" method="POST">
            <div class="mb-3">
                <label class="form-label">Kategori Laporan</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-end-0">
                        <i class="fas fa-tag text-success"></i>
                    </span>
                    <select name="kategori" class="form-select border-start-0" required>
                        <option value="aspirasi" <%= data.kategori === 'aspirasi' ? 'selected' : '' %>>Aspirasi</option>
                        <option value="pengaduan" <%= data.kategori === 'pengaduan' ? 'selected' : '' %>>Pengaduan</option>
                    </select>
                </div>
            </div>

            <div class="mb-4">
                <label class="form-label">Isi Laporan</label>
                <textarea name="deskripsi" class="form-control" rows="6" required placeholder="Tuliskan perubahan laporan Anda..."><%= data.deskripsi %></textarea>
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-save">
                    <i class="fas fa-save me-2"></i> Simpan Perubahan
                </button>
                <a href="/" class="btn btn-cancel">
                    Batal
                </a>
            </div>
        </form>

        <div class="footer-text">
            <strong>Prodi Teknologi Informasi</strong><br>
            Universitas Muhammadiyah Yogyakarta
        </div>
    </div>

</body>
</html>