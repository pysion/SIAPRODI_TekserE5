<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - SIAPPRODI UMY</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Gradient Hijau Tua Khas UMY */
            background: linear-gradient(135deg, #006837 0%, #004d29 100%);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .login-card {
            background: #ffffff;
            border: none;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 400px;
            padding: 40px;
            border-top: 8px solid #f9c909; /* Aksen Kuning/Emas UMY */
        }

        .brand-logo {
            font-size: 2rem;
            font-weight: 800;
            color: #006837; /* Hijau UMY */
            margin-bottom: 5px;
            display: block;
            text-align: center;
        }

        .login-title {
            text-align: center;
            color: #6c757d;
            font-size: 0.85rem;
            margin-bottom: 30px;
            font-weight: 500;
        }

        .form-label {
            font-weight: 600;
            font-size: 0.85rem;
            color: #2d3436;
        }

        .form-control {
            border-radius: 10px;
            padding: 12px 15px;
            border: 1px solid #dee2e6;
        }

        .form-control:focus {
            box-shadow: 0 0 0 4px rgba(0, 104, 55, 0.15);
            border-color: #006837;
        }

        .btn-login {
            background: #006837; /* Tombol Hijau */
            border: none;
            border-radius: 10px;
            padding: 12px;
            font-weight: 700;
            color: #ffffff;
            transition: all 0.3s;
        }

        .btn-login:hover {
            background: #004d29;
            color: #f9c909; /* Teks berubah kuning saat hover */
            transform: translateY(-2px);
        }

        .footer-text {
            text-align: center;
            margin-top: 25px;
            font-size: 0.75rem;
            color: #95a5a6;
            line-height: 1.5;
        }

        .umy-accent {
            color: #f9c909;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <div class="login-card">
        <div class="brand-logo">
            <i class="fas fa-university me-2"></i>SIAP<span style="color: #f9c909;">PRODI</span>
        </div>
        <p class="login-title">Sistem Aspirasi & Pengaduan Prodi</p>

        <form action="/login" method="POST">
            <div class="mb-3">
                <label for="email" class="form-label">Email Mahasiswa / Admin</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-end-0">
                        <i class="fas fa-envelope text-success"></i>
                    </span>
                    <input type="email" name="email" id="email" class="form-control border-start-0" placeholder="user@umy.ac.id" required>
                </div>
            </div>

            <div class="mb-4">
                <label for="password" class="form-label">Kata Sandi</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-end-0">
                        <i class="fas fa-lock text-success"></i>
                    </span>
                    <input type="password" name="password" id="password" class="form-control border-start-0" placeholder="••••••••" required>
                </div>
            </div>

            <button type="submit" class="btn btn-login w-100 shadow-sm">
                Masuk Sekarang <i class="fas fa-arrow-right ms-2"></i>
            </button>
        </form>

        <div class="footer-text">
            Unggul & Islami<br>
            <strong>Fakultas Teknik - UMY</strong>
        </div>
    </div>

</body>
</html>